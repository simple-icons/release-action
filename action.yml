name: 'Simple Icons Release'
description: 'Automatic releases for Simple Icons'
author: 'Eric Cornelissen'

inputs:
  repo-token:
    description: 'The GITHUB_TOKEN secret'
    required: true

runs:
  using: 'node24'
  main: 'lib/index.mjs'
